apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-with-insufficient-cluster-resources
  namespace: failures
  labels:
    app: app-with-insufficient-cluster-resources
spec:
  selector:
    matchLabels:
      app: app-with-insufficient-cluster-resources
  template:
    metadata:
      labels:
        app: app-with-insufficient-cluster-resources
    spec:
      containers:
        - name: test-container
          image: nginx
          resources:
            requests:
              cpu: 11
